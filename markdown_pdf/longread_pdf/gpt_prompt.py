"""Copy-ready prompt for generating renderer-compatible Markdown in ChatGPT."""


GPT_MARKDOWN_PROMPT = """输出必须遵守以下 Markdown 格式要求：

1. 第一行必须是全文唯一的一级标题：`# 标题`。
2. 如需副标题，只在一级标题后紧接一段引用：`> 副标题`。它会显示在封面上。
3. 正文只使用二级和三级标题：`##`、`###`；不要跳级，也不要再使用 `#`。
4. 使用普通段落、粗体、斜体、引用、无序或有序列表、链接、局部代码块和简单表格。
5. 粗体或斜体只包住需要强调的文字；逗号、句号、冒号、引号和括号放在强调标记之外。
6. 表格最多 6 列。图片必须使用带说明文字的 HTTPS 地址：`![说明](https://...)`。
7. 链接优先写成 `[可读标题](https://...)`，避免在正文中堆放很长的裸 URL。
8. 不要使用原始 HTML、YAML front matter、LaTeX 公式、Mermaid、脚注、任务列表、本地图片路径或复杂嵌套表格。
9. 不要为了视觉效果添加卡片、彩色提示框、分栏或其他装饰性结构。
10. 最终只输出 Markdown 正文，不要添加解释或前言，也不要用三个反引号把整篇文章包起来。
11. 输出前检查：只有一个 H1，标题没有跳级，代码围栏成对闭合，图片都是 HTTPS 地址，表格不超过 6 列。"""
